# Rows store.  Cassandra-backed structured row query/write.

_defaults: &defaults
  pubsub_backend: rabbitmq
  rabbitmq_host: localhost
  log_level: INFO

processors:

  - class: graphit.query.rows.cassandra.Processor
    params:
      <<: *defaults
      id: rows-query
      cassandra_host: localhost

  - class: graphit.storage.rows.cassandra.Processor
    params:
      <<: *defaults
      id: rows-write
      cassandra_host: localhost

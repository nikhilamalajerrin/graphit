# Triples store.  Cassandra-backed RDF triple query/write.

_defaults: &defaults
  pubsub_backend: rabbitmq
  rabbitmq_host: localhost
  log_level: INFO

processors:

  - class: graphit.query.triples.cassandra.Processor
    params:
      <<: *defaults
      id: triples-query
      cassandra_host: localhost

  - class: graphit.storage.triples.cassandra.Processor
    params:
      <<: *defaults
      id: triples-write
      cassandra_host: localhost
